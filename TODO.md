# TODO - Reset Password Screen

- [x] Add API error/success display to Forgot Password form.
- [x] Create `NewPasswordComponent` screen (new password + confirm password).
- [x] Add route `/reset-password` mapped to `NewPasswordComponent`.
- [ ] Verify compile/build (blocked in this environment due to PowerShell execution policy).
- [ ] Test end-to-end: request reset → navigate to `/forgot-passwordcheck` → open reset link → `/reset-password?token=...` → update password.


